services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env
    volumes:
      - devlens-data:/data
    environment:
      DATABASE_URL: sqlite:////data/devlens.db

volumes:
  devlens-data:

